# RSI Martingale

RSI-based entry with Martingale position sizing.

| File | What it actually is |
|---|---|
| `RSI Martingale.mq5` | The real strategy (`RSI_MartingaleEA.mq5`): RSI signals with initial lot size / lot multiplier Martingale sizing. |
| `RSI Martingale v2.mq5` | **Not actually RSI Martingale code.** Its header reads `ZeiRangeADX.mq5` - "Consolidation Range ADX-Only Indicator Skeleton" (same Zeiierman origin as `../Consolidation-Range-ADX/`), and the body is a 59-line empty indicator stub with a literal `// TODO: calculation logic` placeholder and no real logic. This looks like a stray/misfiled template rather than a genuine v2 - kept as-is (not deleted) since it wasn't explicitly authorized, but don't expect it to do anything. |
