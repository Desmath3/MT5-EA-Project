# RSI

Base RSI reversal strategy (`RSI_ReversalEA.mq5`) with session filters and
RSI upper/lower level inputs. Closely related to `../RSI-Trend-Trader/`,
which shares the same `RSI_ReversalEA.mq5` origin and input structure (same
session filters, same RSI period/level inputs) but adds inline documentation
comments and further additions on top - not merged here since that wasn't
part of the approved consolidation pass, but they're clearly the same
starting point.
